<%= form_for(@time_off) do |f| %>
  <% if @time_off.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_off.errors.count, "error") %> prohibited this time_off from being saved:</h2>

      <ul>
      <% @time_off.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :startDate, "Start Date" %><br>
    <%= f.date_field :startDate %>
  </div>
  <div class="field">
    <%= f.label :endDate, "End Date" %><br>
    <%= f.date_field :endDate %>
  </div>
  <div class="field">
    <%= f.label :approved %><br>
    <%= f.text_field :approved %>
  </div>
  <div class="field">
    <%= f.label :comments %><br>
    <%= f.text_field :comments %>
  </div>
  <div class="field">
    <%= f.label :provider_id, "Provider" %><br>
     <%= f.select( :provider_id, options_for_select(Provider.order('"lName"').all.collect{|a| [a.lName + ", " + a.fName , a.id]}, params[:provider_id])) %> 
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
