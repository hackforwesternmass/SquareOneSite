<p id="notice"><%= notice %></p>

<h3>Attendance </h3>

<table border>
  <thead>
    <tr>
      <th>Child</th>
      <th>Provider</th>
      <th>Termination date</th>
      <th>Date of Attendance</th>
      <th>Time In</th>
      <th>Time Out</th>
      <th>Reason Absent</th>
      <th>Created by</th>
      <th>Date created</th>
      <th>Last Edited by</th>
      <th>Date edited</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @attendances.each do |attendance| %>
      <tr>
        <td><%= attendance.child.lName %></td>
        <td><%= attendance.provider.lName %></td>
        <td><%= attendance.childTermDate %></td>
        <td><%= attendance.dateOfService %></td>
        <td><%= attendance.timeIn %></td>
        <td><%= attendance.timeOut %></td>
        <td><%= attendance.absent %></td>
        <td><%= attendance.whoCreated %></td>
        <td><%= attendance.dateCreated %></td>
        <td><%= attendance.whoEdited %></td>
        <td><%= attendance.dateEdited %></td>
        <!--
       <td> link_to 'Show', attendance %></td>
        <td> link_to 'Edit', edit_attendance_path(attendance) %></td>
        <td> link_to 'Destroy', attendance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    -->
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- %= link_to 'New Attendance', new_attendance_path %> %>
